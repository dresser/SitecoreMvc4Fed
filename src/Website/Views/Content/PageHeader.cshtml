@model Sitecore.Mvc.Presentation.RenderingModel
<div class="callout large primary">
    <div class="row column text-center">
        <h1>@Html.Sitecore().Field("Title")</h1>
        <h2 class="subheader">@Html.Sitecore().Field("SubTitle")</h2>
    </div>
</div>
